import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Ensure TypeScript path aliases work correctly
  typescript: {
    // We'll let TypeScript handle type checking
  },
};

export default nextConfig;
